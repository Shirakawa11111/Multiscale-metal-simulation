# Adversarial scrutiny verdict (4-expert workflow)

## Verdict: artifact-fix-first

## Decisive experiment
ABANDON pairwise. Run the minimal multi-slip COLL on/off discriminator, the one experiment that cannot return ambiguous because it toggles exactly one bit on the effect's home turf. GEOMETRY: small FCC cell, full PBC, DDD_FFT + Collision + Topology, FCC_0 mobility, STRAIN-RATE control (edot ~1e2-1e3 /s) so flow stress is an OUTPUT (not stress-on-m-only). MOBILE MODEL: finite Frank-Read sources that MULTIPLY (NOT infinite lines) on >=2 collinear-partner slip systems both driven near-equal Schmid, so carriers replenish and collinear encounters are bidirectional. CRITICAL: enable cross_slip (currently None everywhere -- without it the replenishment feedback the mechanism needs does not exist). OBSERVABLE: steady-state flow stress tau_flow and stored-network MFP L_mf = 1/(b*drho_stored/dgamma), measured COLL=1 (annihilation enabled, opposite-sense collinear partner) vs COLL=0 (same Burgers family/density but annihilation suppressed via same-sense), at matched as-built rho and matched applied loading. The ONLY toggle is annihilation on/off -- this differences out bow-out/multiplication/remesh common-mode that swamped all prior attempts. rho per system in {3e12, 3e13} m^-2 (bracket the predicted threshold MFP<L_source). tau: set by strain-rate control, not imposed. box: large enough that geometric MFP 1/sqrt(a_coll*rho) fits pre-wrap at 3e13 (LBOX >= ~15000b). REQUIRED GATES before reading: (i) forest/cell density-drift control <5%; (ii) carrier-symmetry -- surviving mobile density and gamma track between COLL=1 and COLL=0 across the fit window; (iii) reach a flow plateau (gamma > 1e-3, steady-state, not a transient). CONFIRM collinear-MFP dominance: COLL=1 shortens L_mf and RAISES tau_flow vs COLL=0, the gap GROWS with rho (density threshold), and tau_flow(collinear-duplex)/tau_flow(glissile-duplex run identically) rises toward the canonical sqrt(0.62/0.12)~2.3x. REFUTE: at a plateau, with all three gates passed, COLL=1 == COLL=0 in both L_mf and tau_flow at BOTH densities -- then the canonical a_coll~0.62 does not reproduce in this ExaDiS/FCC_0 setup at all.

## Stop condition
Declare an HONEST NEGATIVE -- collinear dominance is NOT a pairwise MFP effect, it is an irreducibly multi-slip collective phenomenon -- and stop digging on the pairwise track the moment EITHER holds: (A) the multi-slip COLL=1-vs-0 cell, passing all three gates (density-drift <5%, carrier-symmetry, gamma>1e-3 plateau), shows tau_flow and L_mf statistically identical (within seed CI) at BOTH 3e12 and 3e13 with no density trend -> the effect does not reproduce in this DDD setup; publish the bounded negative. OR (B) the multi-slip cell shows a clear COLL-on density-scaling tau_flow / L_mf-shortening -> the effect IS real and lives in multi-slip, which by itself confirms the pairwise null was a geometry/definition mismatch, not a measurement to be salvaged. KEEP DIGGING (and only here) if a gate FAILS -- e.g. cross-slip cannot sustain collinear replenishment, or the bow-out baseline mu*b/L_source swamps the density slope as it did in SYNTHESIS sec7 -- because then the run is uninterpretable, exactly as attempt-3 is now. Do NOT run another pairwise density/glide scan: every cross-examination showed it trades the glide-starvation confound for a carrier-divergence or forest-self-annihilation confound and cannot come back clean. The attempt-3 'rel d_mob = +0.0%' must be retracted as sampling-starved (xbar=0.31 forest cells, code-verified) and computed on the demoted rho_removed_balance estimator without the design's opp-minus-same events subtraction (analyze() line 646), NOT reported as a null against Madec/Devincre.

## Go multi-slip: yes

## One line
The attempt-3 null is an artifact (0.31-forest-cell glide, read off the demoted balance estimator with no opp-minus-same subtraction) and must be retracted, but do NOT re-run the pairwise scan -- the geometry structurally cannot host a co-driven multi-slip coefficient (collinear b3=0, no storage channel, no replenishment), so move directly to the multi-slip COLL on/off cell, the single test that cannot return ambiguous.